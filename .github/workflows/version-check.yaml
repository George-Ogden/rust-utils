name: Check Cargo.toml version matches version.txt

on:
  push:
    branches:
      - m
  pull_request:
    branches:
      - m

jobs:
  check-cargo-toml:
    uses: George-Ogden/actions/.github/workflows/version-check.yaml@v4.6.3
    with:
      prefix: version = \"
      filename: Cargo.toml
